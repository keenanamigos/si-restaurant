<div class="container">
	<p class="lead center white">Please select from the following categories:</p>
	<div class="wrapper">
    <div class="dropdown center">
  		<button class="btn btn-success dropdown-toggle" type="button" id="categories-options" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"> 
  			Categories
    		</button>
    		<div class="dropdown-menu">
          <% @category.each do |c| %>
            <li><%= c.title %></li>
          <% end %>
    		</div>
  </div>
	</div>
</div>